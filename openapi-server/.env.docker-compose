#
# Used for local docker-compose configuration
#

# Database
DB_URL=postgresql://postgres:password@postgres:5432/electric?sslmode=disable
DB_PORT=5432
DB_NAME=electric
DB_USERNAME=postgres
DB_PASSWORD=password

# Electric
ELECTRIC_PORT=4000

# = = = = =
