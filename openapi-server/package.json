{
  "name": "openapi-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "copy-env": "cat .env.example .env.docker-compose > .env",
    "lqb:test": "pnpm exec dotenv -e .env -- bash -c 'echo $DB_JDBC_URL'",
    "lqb:status": "pnpm exec dotenv -e .env -- mvn liquibase:status",
    "lqb:update": "pnpm exec dotenv -e .env -- mvn liquibase:update",
    "lqb:dropAll": "pnpm exec dotenv -e .env -- mvn liquibase:dropAll",
    "lqb:rollback": "pnpm exec dotenv -e .env -- mvn liquibase:rollback",
    "lqb:validate": "pnpm exec dotenv -e .env -- mvn liquibase:validate",
    "test:unit": "pnpm exec dotenv -e .env -- mvn test",
    "test:it": "pnpm exec dotenv -e .env -- mvn failsafe:integration-test",
    "test:verify": "pnpm exec dotenv -e .env -- mvn verify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.3",
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
